name: agora
description: "A simple Flutter demo app."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  agora_rtc_engine: ^6.5.2
  flutter:
    sdk: flutter
  permission_handler: ^12.0.1
  flutter_screenutil: ^5.9.0
  agora_chat_sdk: ^1.3.3
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - .env
